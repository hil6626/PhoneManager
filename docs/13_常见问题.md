# 13 常见问题 (合并旧docs/各类问题解析.md)

| 问题 | 原因 | 解决 | 防护 |
|------|------|------|------|
| 号码重复判断错误 | 格式不统 (空格/前缀) | strip(), re 11位 | 输入sanitize func |
| 批量导入中断 | 空行/格式错 | 逐行try-catch | 验证CSV header |
| exe无法访问DB/资源 | pack遗漏 | --add-data "database;database" | spec文件固定 |
| Excel导出失败 | lib version | openpyxl==3.1.2 | requirements pin |
| DB锁/并发 | 多实例 | single run or lock timeout | app lock file |
| 性能慢 (10k+) | no index | CREATE INDEX idx_phone | init DB script |

**旧内容合并**：各类问题解析.md → 此表，无重复。

**提醒**：rm docs/各类问题解析.md (沉积清理).

| exe numpy crash | pandas hidden-import miss | spec加 --hidden-import=pandas.np | pyinstaller --debug=imports test |
| DB not found in exe | add-data遗漏 | spec datas=[('database','database')] | 验证dist/database/data.db存在 |

**优化**：FAQ search UI (未来).
